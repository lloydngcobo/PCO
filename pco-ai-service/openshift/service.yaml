apiVersion: v1
kind: Service
metadata:
  name: pco-ai-service
  labels:
    app: pco-ai-service
    component: ai-service
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: pco-ai-service