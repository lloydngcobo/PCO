apiVersion: v1
kind: Secret
metadata:
  name: pco-credentials
  labels:
    app: pco-api-wrapper
type: Opaque
stringData:
  # Replace these with your actual PCO credentials
  # Or use: oc create secret generic pco-credentials --from-literal=app-id=YOUR_APP_ID --from-literal=secret=YOUR_SECRET
  app-id: "REPLACE_WITH_YOUR_PCO_APP_ID"
  secret: "REPLACE_WITH_YOUR_PCO_SECRET"