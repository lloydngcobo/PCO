apiVersion: v1
kind: Service
metadata:
  name: pco-api-wrapper
  labels:
    app: pco-api-wrapper
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: pco-api-wrapper